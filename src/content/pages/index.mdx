---
title: Website Core Template
description: Minimal Astro 5 template for multilingual static sites by Affiliate.FM
---

import { products } from "@/data/products";

# Website Core Template

Minimal static site generator with i18n, auto-sitemap, and updatable core.

## Quick Start

```bash
npm create astro@latest -- --template affiliatefm/website-core-template
make dev
```

## Data Example

<table>
  <thead>
    <tr><th>Product</th><th>Description</th><th></th></tr>
  </thead>
  <tbody>
    {products.map(p => (
      <tr key={p.id}>
        <td><strong>{p.name}</strong></td>
        <td>{p.description}</td>
        <td><a href={p.url}>View →</a></td>
      </tr>
    ))}
  </tbody>
</table>

---

