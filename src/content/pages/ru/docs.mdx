---
# general
title: Документация — Website Core Template
description: Узнайте, как настроить, сконфигурировать и расширить website-core-template для вашего многоязычного статического сайта.
# ai-translator
_ai-translator:
  source: docs.mdx
  hash: 94f0234d7456
  model: gpt-4.1
  date: 2025-12-21
---

# Документация

<p class="tpl-muted mb-6 text-sm">Website Core Template — от Affiliate.FM — 5 минут на прочтение</p>

В этом руководстве описано, как настроить систему шаблонов, выбрать стили и расширить ядро.

## Содержание

1. [Структура проекта](#project-structure)
2. [Конфигурация](#configuration)
3. [Система шаблонов](#template-system)
4. [Стили оформления](#style-presets)
5. [Глобальные компоненты](#global-components)
6. [Добавление контента](#adding-content)
7. [Многоязычная настройка](#multilingual-setup)
8. [Компоненты шаблонов](#template-components)

---

## Структура проекта

```
src/
├── components/global/ # Общие компоненты (head/body)
│   ├── head/
│   └── body/
├── content/pages/     # Ваш контент (MDX-файлы)
│   ├── index.mdx      # Главная страница
│   ├── docs.mdx       # Документация
│   ├── _collections/  # Элементы коллекций (страницы продуктов)
│   ├── software/      # Страница коллекции
│   └── websites/      # Страница коллекции
├── data/              # Ваша конфигурация
│   ├── site.ts        # URL сайта, языки, выбор шаблона
│   └── ui.ts          # Переводы интерфейса
├── templates/         # Шаблоны и системы стилей
│   ├── registry.ts    # Зарегистрированные шаблоны
│   └── affiliate/     # Пример шаблона
│       ├── Layout.astro
│       ├── template.ts
│       ├── styles/
│       ├── pages/
│       └── components/
├── i18n/              # Вспомогательные функции для URL
├── integrations/      # Проверки сборки
└── pages/             # Роутер Astro
```

## Конфигурация

Редактируйте `src/data/site.ts`:

```typescript
export const siteUrl = "https://your-domain.com";
export const locales = ["en", "ru"] as const;
export const defaultLocale = "en";

export const template = {
  id: "affiliate",
  style: "paper",
} as const;
```

Редактируйте `src/data/ui.ts` для общих строк интерфейса:

```typescript
export const uiStrings = {
  en: { siteName: "My Site", homeLabel: "Home" },
} as const;
```

## Система шаблонов

Шаблоны — это отдельные папки в `src/templates/`.

- `Layout.astro` — глобальный обёртчик для всех страниц.
- `pages/` содержит шаблоны страниц, такие как `Article.astro` и `Product.astro`.
- `components/` — UI-блоки, специфичные для шаблона.
- `styles/` — пресеты стилей для шаблона.
- `template.ts` — описание метаданных шаблона (страницы, стили, путь к layout).

Чтобы зарегистрировать новый шаблон:

1. Добавьте папку в `src/templates/<your-template>/`.
2. Создайте `template.ts` с полями `id`, `layout`, `pages` и `styles`.
3. Добавьте шаблон в `src/templates/registry.ts`.
4. Выберите его в `src/data/site.ts`.

## Стили оформления

Каждый шаблон может содержать несколько пресетов стилей. Выберите нужный в `src/data/site.ts`:

```typescript
export const template = {
  id: "affiliate",
  style: "studio",
} as const;
```

Чтобы добавить новый стиль:

1. Добавьте новый идентификатор стиля в `src/templates/<template>/template.ts`.
2. Создайте отдельный CSS-файл в `src/templates/<template>/styles/<style>.css`.
3. Определите переменные под `:root[data-template="<template>"][data-style="<style>"]`.
4. (Необязательно) Импортируйте `./base.css` для общих правил.

Каждый файл стиля изолирован, и в сборку попадает только выбранный. Стили могут не только переопределять переменные, но и расширять или изменять базовый CSS.

## Глобальные компоненты

Глобальные компоненты находятся в `src/components/global/` и не зависят от шаблона.

- `head/` — для мета-тегов, структурированных данных и другого содержимого `<head>`.
- `body/` — для общих элементов body, например, помощников по доступности.

Шаблоны могут импортировать эти компоненты и использовать их без дублирования логики.

## Добавление контента

Создавайте MDX-файлы в `src/content/pages/`:

```yaml
---
title: "Page Title"
description: "Meta description for SEO"
---

# Ваш контент здесь
```

### Опции frontmatter

| Поле | Тип | Описание |
|------|-----|----------|
| title | string | Заголовок страницы (обязателен) |
| description | string | Мета-описание |
| updatedAt | date | Для sitemap |
| draft | boolean | Исключить из сборки |
| link | string | Внешний URL (страницы продуктов) |
| pros | string[] | Список плюсов (страницы продуктов) |
| cons | string[] | Список минусов (страницы продуктов) |

## Многоязычная настройка

### Одинаковые слаги (автоматически)

```
pages/about.mdx      -> /about/
pages/ru/about.mdx   -> /ru/about/
```

Hreflang-ссылки генерируются автоматически.

## Компоненты шаблонов

Компоненты, специфичные для шаблона, импортируются из `src/templates/<template>/components`:

```mdx
import RatingTable from "@/templates/affiliate/components/RatingTable.astro";

<RatingTable collection="software" limit={2} />
```

---

## Дальнейшие шаги

1. Настройте `src/data/site.ts`
2. Выберите шаблон и стиль
3. Добавьте свой контент в `src/content/pages/`
4. Разверните сайт на вашей любимой платформе

**Вопросы?** Откройте issue на [GitHub](https://github.com/affiliatefm/website-core-template).

---

[Назад на главную](/)