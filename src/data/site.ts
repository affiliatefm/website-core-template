/**
 * Site Configuration
 * ==================
 * The main configuration file for your site.
 * This is a USER file — never overwritten by `make update`.
 */

/**
 * Your production site URL.
 * Used for sitemap, canonical URLs, and hreflang tags.
 * 
 * Examples:
 *   - "https://example.com"
 *   - "https://www.mysite.org"
 *   - "https://blog.company.io"
 */
export const siteUrl = "https://example.com";

/**
 * Default language code (BCP 47 format).
 * 
 * Content for this language lives directly in src/content/pages/
 * Other languages use subfolders: src/content/pages/ru/, src/content/pages/de/, etc.
 * 
 * Common codes:
 *   - "en" — English
 *   - "en-US" — American English
 *   - "ru" — Russian
 *   - "de" — German
 *   - "ja" — Japanese
 *   - "zh-CN" — Simplified Chinese
 */
export const defaultLanguage = "en";
