/**
 * Site Configuration
 * ===================
 * The only file you MUST edit before deploying.
 */

/** Your production site URL (used for sitemap, canonical URLs, hreflang) */
export const siteUrl = "https://example.com";

/** 
 * Default language code (BCP 47 format).
 * Content for this language lives directly in src/content/pages/.
 * Other languages use subfolders like src/content/pages/ru/, src/content/pages/de/, etc.
 */
export const defaultLanguage = "en";
