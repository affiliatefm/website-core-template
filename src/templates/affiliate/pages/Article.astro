---
/**
 * Article Page
 * ============
 * Clean article layout for blog posts and guides.
 */

import type { CollectionEntry } from "astro:content";

interface Props {
  entry: CollectionEntry<"pages">;
}

const { entry: _entry } = Astro.props;
---

<article>
  <slot />
</article>
